//  定义一个AudioOption类，包含name和src两个属性
class AudioOption {
  final String name; //  name属性，表示音频的名称
  final String src; //  src属性，表示音频的路径
  
  //  构造函数，初始化name和src属性
  const AudioOption(
    this.name,
    this.src,
  );
}
